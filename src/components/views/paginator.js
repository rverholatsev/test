import React from 'react';
import PropTypes from 'prop-types';

export default React.createClass({
    propTypes: {
        page: PropTypes.integer.isRequired,
        maxPage: PropTypes.integer.isRequired,
    },
    getInitialState: function () {
    },
    render: function () {
        return (
            <div>

            </div>
        );
    }
});